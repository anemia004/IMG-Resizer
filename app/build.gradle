plugins {
    id 'com.android.application'
}

android {
    namespace 'com.example.imgresizer'
    compileSdk 34

    defaultConfig {
        applicationId "com.example.imgresizer"
        minSdk 21
        targetSdk 34
        versionCode 1
        versionName "1.0"
    }

    signingConfigs {
        debug {
            storeFile file("${System.getProperty("user.home")}/.android/debug.keystore")
            storePassword "android"
            keyAlias "androiddebugkey"
            keyPassword "android"
        }
    }

    buildTypes {
        release {
            minifyEnabled false
            signingConfig signingConfigs.debug
        }
    }
}

dependencies {
    implementation 'androidx.core:core:1.13.1'   // required for permission helpers
}
